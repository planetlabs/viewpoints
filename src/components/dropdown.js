var React = require('react');
var Button = require('react-bootstrap').Button;

var Dropdown = React.createClass({
  render: function() {
    return <Button>I am a dropdown, I guess!</Button>;
  }
});

module.exports = Dropdown;
